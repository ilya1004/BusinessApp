<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.web.WebView?>
<BorderPane fx:id="rootPane" xmlns:fx="http://javafx.com/fxml"
            fx:controller="oll.businessdesktop.ProcessDesignerController">
    <top>
        <VBox>
            <HBox spacing="10" alignment="CENTER_LEFT" style="-fx-padding: 8 15 0 15;">
                <Label fx:id="diagramNameLabel" style="-fx-font-size: 14px; -fx-font-weight: bold;"/>
                <Label fx:id="lastSavedLabel" style="-fx-font-size: 11px; -fx-text-fill: -color-fg-muted;"/>
            </HBox>
            <ToolBar fx:id="toolBar">
                <Button text="Новая диаграмма" onAction="#onNewDiagram"/>
                <Button text="Сохранить" onAction="#onSaveXML"/>
                <Separator/>
                <Button text="Открыть .bpmn" onAction="#onOpenFile"/>
                <Button text="Открыть из БД" onAction="#onOpenFromDb"/>
                <Separator/>
                <Button text="Экспорт в .SVG" onAction="#onExportSVG"/>
                <Button text="Открыть папку SVG" onAction="#onOpenSvgFolder"/>
                <Region HBox.hgrow="ALWAYS"/>
            </ToolBar>
        </VBox>
    </top>
    <center>
        <WebView fx:id="webView"/>
    </center>
    <right>
        <ScrollPane fitToWidth="true" styleClass="properties-panel" prefWidth="320" minWidth="320">
            <VBox spacing="10" style="-fx-padding: 15;">
                <Label text="Свойства" style="-fx-font-size: 14px; -fx-font-weight: bold;"/>
                <Separator/>
                <VBox fx:id="propertiesContainer" spacing="8">
                    <Label fx:id="noSelectionLabel" text="Выберите элемент" style="-fx-text-fill: -color-fg-muted;"/>
                </VBox>
            </VBox>
        </ScrollPane>
    </right>
</BorderPane>
